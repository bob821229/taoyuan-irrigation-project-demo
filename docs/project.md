需求:
    1.把所有彈窗內的資料，都移到 script 內，把整個湖口站的資訊都放在同一個obj內，不要直接放在template




修改紀錄:
    2026-05-20
    1. 將第一層互動改為監聽內嵌 SVG 的 id，不再使用透明座標熱區。
    2. 點擊 id="ISTD303009001" 時開啟詳細資料彈窗。
    3. 點擊 id="ISTD303009001_door" 時使用 vue-router 跳轉到第二層 /hukou。
    4. 第二層改為監聽 id="ISTD603009001_door"，點擊後跳轉到第三層 /hukou/guangfu-1。
    5. 在 CSS 中替可點擊 SVG id 加上 pointer 游標與 hover 高亮效果。
    6. 已執行 npm run build，編譯成功。
    7. 依最新需求將詳細資料彈窗頁籤改為「基本資訊、埤塘資訊、作物資訊」三個頁籤，移除「用水資訊」。
    8. 更新基本資訊、埤塘資訊、作物資訊三個頁籤的欄位與數值。
    9. 調整作物資訊長文字排版，避免內容擠壓。
    10. 將 SVG hover 效果由強藍色光暈改為較收斂的墨綠色細描邊與淡陰影。
    11. 已再次執行 npm run build，編譯成功。
    12. 依最新需求縮小詳細資料彈窗內文字字級，並加寬彈窗讓欄位標題盡量不換行。
    13. 將 SVG hover 效果改為粉紅色淡陰影。
    14. 取消 id="ISTD303009001" 子元素描邊，避免「湖口站」文字被高亮；保留站點外框/整體外光效果。
    15. door icon 仍保留粉紅色細描邊，讓可跳轉區域清楚可辨。
    16. 已再次執行 npm run build，編譯成功。
    17. 依最新需求調整彈窗清單為 grid 對齊，dt 欄位靠右、dd 欄位靠左。
    18. 將 dd 文字改為與標題同級字體大小並僅保留粗體，不再放大顯示。
    19. 在基本資訊、埤塘資訊、作物資訊三個頁籤右下角新增「+」展開按鈕，展開後可再次點擊收合。
    20. 基本資訊展開後顯示灌溉小組表格。
    21. 埤塘資訊展開後顯示池塘蓄水量與蓄水率表格。
    22. 作物資訊展開後依 docs/1.png 形式加入農試所與農糧署兩段表格，表格區可水平捲動。
    23. 已再次執行 npm run build，編譯成功。
    24. 依最新需求新增 src/data/irrigationDetails.js，將彈窗摘要與三組表格資料從 HomeView.vue 抽離。
    25. 將基本資訊、埤塘資訊、作物資訊的摘要清單與表格內容改為使用 v-for 渲染。
    26. 將彈窗內容標題改為全部靠左對齊。
    27. 表格視覺改為簡約樣式：淡色表頭、細分隔線、斑馬紋列與 hover 淡底色。
    28. 已再次執行 npm run build，編譯成功。
    29. 依最新需求移除 src/data/irrigationDetails.js，將湖口站彈窗資料集中到 HomeView.vue 的 script setup 內。
    30. 新增單一 hukouStationInfo 物件，統一管理彈窗標題、頁籤、摘要資料、展開按鈕文字、表格表頭與表格列資料。
    31. Template 不再直接放彈窗資料文字，改由 hukouStationInfo 搭配 v-for 渲染所有頁籤摘要與表格。
    32. 已再次執行 npm run build，編譯成功。
